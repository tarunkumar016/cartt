@Library('roboshop-shared-library') _

pipeline {
    agent any
    stages {

        stage('Performing Lint Checks') {
            steps {
                scripts {
                    sample.info('SharedLlibrary' , 'stage.google.com')
                }
            }
        }
    }
}